For future reference:

For the extra UHook functions, the information comes from IDa decompilations.

UStruct does not have the begenning padding or the BaseChain member.
FFieldPath has the Path member at 0x20 and the padding does not exist.

The DLSS blueprint library assertions are missing? who knows.

UStruct::IsSubclassOf will have to be re-implemented

TUObjectArray had extra padding at beginning?